[pytest]
# Pytest configuration file

# Test discovery patterns
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Output options
addopts = 
    -v
    --strict-markers
    --tb=short
    --color=yes

# Markers for organizing tests
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow running tests
    gateway: API Gateway layer tests
    middleware: Middleware component tests
    handler: Handler layer tests
    voice: Voice processing tests (STT/TTS)
    api: API endpoint tests
    service: Service layer tests
    agent: Agent tests
    auth: Authentication tests

# Environment variables
env =
    TESTING=1
    AUTOMEM_URL=http://localhost:8001

# Coverage options
[coverage:run]
source = app
omit = 
    */tests/*
    */venv/*
    */__pycache__/*
    */migrations/*

[coverage:report]
precision = 2
show_missing = True
skip_covered = False
