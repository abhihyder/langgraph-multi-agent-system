# Example .env file
# Copy this to .env and fill in your API keys

# OpenAI Configuration (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Alternative: Anthropic (if you want to use Claude)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Model Configuration (optional - defaults to gpt-4)
# BOSS_MODEL=gpt-4
# RESEARCH_MODEL=gpt-4
# WRITING_MODEL=gpt-4
# CODE_MODEL=gpt-4
# AGGREGATOR_MODEL=gpt-4

# Temperature Settings (optional)
# BOSS_TEMPERATURE=0
# RESEARCH_TEMPERATURE=0.3
# WRITING_TEMPERATURE=0.7
# CODE_TEMPERATURE=0.2
# AGGREGATOR_TEMPERATURE=0.5

# System Configuration (optional)
# MAX_RETRIES=3
# TIMEOUT=120
# LOG_LEVEL=INFO
# VERBOSE=false

# MCP Server Configuration (Optional - for external tool calling)
# Uncomment and configure to enable external tools for research agent
#
# Example 1: Brave Search MCP Server (web search capability)
# MCP_SERVER_COMMAND=npx
# MCP_SERVER_ARGS=-y @modelcontextprotocol/server-brave-search
#
# Example 2: File System MCP Server (local file access)
# MCP_SERVER_COMMAND=npx
# MCP_SERVER_ARGS=-y @modelcontextprotocol/server-filesystem /path/to/allowed/directory
#
# Example 3: Custom Python MCP Server
# MCP_SERVER_COMMAND=python
# MCP_SERVER_ARGS=/path/to/your/mcp_server.py
#
# Note: MCP integration is optional. The system works fully without it.
# When configured, the research agent can use external tools for enhanced research.
